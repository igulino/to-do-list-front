# O destino de /api no desenvolvimento e preview fica em vite.config.ts.

# Em produção, deixe vazio ao servir a API no mesmo domínio.
# Para uma API em outra origem do mesmo site, configure CORS_ORIGIN no backend.
VITE_API_URL=
